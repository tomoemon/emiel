{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "if": "Bash(git commit *)",
        "hooks": [
          {
            "type": "command",
            "command": "pnpm run fmt && pnpm run lint && pnpm run test",
            "timeout": 120,
            "statusMessage": "Running fmt, lint and test..."
          }
        ]
      }
    ]
  }
}
