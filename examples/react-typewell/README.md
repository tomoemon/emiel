# 概要

タイプウェル風の複数ワード連続入力サンプル。

- 半角スペース区切りで全ワードを一行表示し、現在ワードをハイライトする
- 各ワードの所要時間・KPM・ミス数を終了後に集計表示する
- ローマ字ルールと direct input ルールを `merge` することで、末尾「ん」をスペースで `n` 1 打確定できる（タイプウェル本家仕様）

# 動かし方

```
pnpm install
pnpm dev
```
